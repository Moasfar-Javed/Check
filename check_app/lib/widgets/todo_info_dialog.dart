import 'package:flutter/material.dart';
import '../services/todo_model.dart';

class TodoInfoDialog extends StatelessWidget {
  final Todo todo;
  const TodoInfoDialog({super.key, required this.todo});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}